[build-system]
requires = [
    "setuptools",
    "wheel",
    "setuptools_scm",
    "cython",
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v -ra"
